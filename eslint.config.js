// L'override e2e/** (no-explicit-any + no-unused-vars off) est intégré à la
// config partagée depuis dev-pwa-config 1.3.0 — simple réexport ici.
export { default } from '@mister-guiiug/dev-pwa-config/eslint-react';
